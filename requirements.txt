fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0
pydantic>=2.5
numpy>=1.26
faiss-cpu>=1.7
aiosqlite>=0.19
tiktoken>=0.5
sentence-transformers>=2.2

# --- Optional backends / integrations (install only when the feature is on) ---
# lancedb>=0.33        # enables memory.vector_backend: lancedb (Directive 3)
# pyarrow>=14          # lancedb dependency (schema); usually pulled automatically
# teich                # tracing -> SFT dataset formatting (Directive 4); alpha, pin once confirmed
